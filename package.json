{
  "name": "organic-food",
  "private": true,
  "description": "Site Organic Food — Cloudflare Pages",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "bash scripts/build.sh && node scripts/verify.js",
    "dev": "npm run build && wrangler pages dev dist --port 8788",
    "deploy": "npm run build && wrangler deploy",
    "deploy:pages": "npm run build && wrangler pages deploy dist --project-name=organic-food --branch=main"
  },
  "devDependencies": {
    "wrangler": "^4.100.0"
  }
}
